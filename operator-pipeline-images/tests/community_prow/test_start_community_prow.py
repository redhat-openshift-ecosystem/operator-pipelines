from operatorcert.community_prow.start_community_prow import initiate_community_prow


# placeholder test for initial entrypoint
def test_initiate_community_prow():
    assert "Hello world" == initiate_community_prow()
