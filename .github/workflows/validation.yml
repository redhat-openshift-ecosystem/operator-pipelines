---
name: validation

on:  # yamllint disable-line rule:truthy
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  tox:
    name: Run unit tests and linters
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: fedora-python/tox-github-action@v0.4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pdm sync -dG operatorcert-dev
          pdm sync -G operatorcert
          pdm sync -dG tox

      - name: Run Tests
        run: |
          pdm run -v tox
